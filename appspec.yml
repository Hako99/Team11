
version: 0.0
os: linux

files:
  - source: /
    destination: /home/ec2-user/tabling

permissions:
  - object: /home/ec2-user/tabling/
    owner: ubuntu
    group: ubuntu

hooks:
  AfterInstall:
    - location: deploy.sh
      timeout: 60
      runas: ubuntu